/**
 * GitHub Pages 등 정적 호스팅용 — Gemini API는 Worker 프록시를 경유합니다.
 * 로컬만 쓸 때: proxyUrl 을 "" 로 두고 node server.mjs 실행
 */
window.DATA_AI_CONFIG = {
  proxyUrl: "https://gemini-proxy.korea419.workers.dev",
};
